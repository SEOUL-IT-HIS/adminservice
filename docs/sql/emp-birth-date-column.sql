-- =========================================================
-- 직원 생년월일 — EMPLOYEE 테이블 컬럼 추가
-- SQLGate로 직접 실행 예정 (이 저장소 코드로는 반영되지 않음 — 기록 목적)
--   - BIRTH_DATE : 등록 시 입력한 주민등록번호 앞자리에서 추출해서 저장
--     (원본 주민등록번호는 어디에도 저장하지 않음 — RRN_HASH만 남는다)
-- =========================================================

ALTER TABLE EMPLOYEE ADD (
    BIRTH_DATE DATE
);
