-- =========================================================
-- IH2-107 직원 우편번호/주소 — EMPLOYEE 테이블 컬럼 추가
-- SQLGate로 직접 실행 예정 (이 저장소 코드로는 반영되지 않음 — 기록 목적)
--   - ZIP_CODE      : 우편번호 (다음 주소 검색 결과, 5자리)
--   - ADDRESS       : 기본주소 (다음 주소 검색 결과)
--   - ADDRESS_DETAIL: 상세주소 (사용자가 직접 입력, 예: 101동 202호)
-- =========================================================

ALTER TABLE EMPLOYEE ADD (
    ZIP_CODE       VARCHAR2(10),
    ADDRESS        VARCHAR2(200),
    ADDRESS_DETAIL VARCHAR2(200)
);
