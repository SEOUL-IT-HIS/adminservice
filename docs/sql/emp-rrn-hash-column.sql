-- =========================================================
-- 직원 주민등록번호 중복체크 — EMPLOYEE 테이블 컬럼 추가
-- SQLGate로 직접 실행 예정 (이 저장소 코드로는 반영되지 않음 — 기록 목적)
--   - RRN_HASH : 주민등록번호 원본이 아니라 HMAC-SHA256 해시값만 저장 (64자 hex)
--   - 평문 주민등록번호는 어떤 컬럼에도 저장하지 않는다
-- =========================================================

ALTER TABLE EMPLOYEE ADD (
    RRN_HASH VARCHAR2(64)
);
